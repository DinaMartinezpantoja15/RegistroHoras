@* @model IEnumerable<RegistroHoras.Models.Ingreso> *@
@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
     <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <meta name="viewport" content="width=device-width" />
    <title>Detalles de Ingreso</title>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-12 text-aling center">
                <form id="formCerrarSesion" asp-controller="Auth" asp-action="Logout" method="post">
                    <button type="submit" class="btn btn-primary mt-5">Cerrar Sesión</button>
                </form>

             <h4 class="mt-5">Detalles de Ingreso</h4>
        <hr />
        <h1>Ingreso con exito</h1>

        <form asp-controller="Horas" asp-action="MarcarHora" method="post">
            <button type="submit">Marcar Hora</button>
        </form>
        
      @*   @foreach (var ingreso in Model)
        {
        <dl class="row">
            <dt class="col-sm-2">
                <p>Hora de ingreso: @ingreso.Hora_Ingreso</p>
            </dt>
            <dd class="col-sm-10">
                <p>Fecha de ingreso: @ingreso.Fecha_Ingreso</p>
            </dd>
        </dl>
        } *@
        </div>
    </div>
    <div>
        
    </div>
</body>
</html>
